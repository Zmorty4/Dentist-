export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const{name,phone,service,comment=''}=req.body||{};if(!name||!phone||!service)return res.status(400).json({error:'Заполните обязательные поля'});const token=process.env.TELEGRAM_BOT_TOKEN;const chatId=process.env.TELEGRAM_CHAT_ID;if(!token||!chatId)return res.status(500).json({error:'Telegram не настроен'});const text=['🦷 Новая заявка','',`👤 Имя: ${name}`,`📞 Телефон: ${phone}`,`🩺 Услуга: ${service}`,`💬 Комментарий: ${comment||'Не указан'}`].join('\n');try{const r=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chat_id:chatId,text})});const j=await r.json();if(!r.ok||!j.ok)return res.status(502).json({error:j.description||'Ошибка Telegram'});return res.status(200).json({ok:true})}catch(e){return res.status(500).json({error:'Внутренняя ошибка сервера'})}}